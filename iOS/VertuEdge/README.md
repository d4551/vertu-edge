# VertuEdge iOS

SwiftUI + XCTest scaffold for iOS parity.

## Build tests

```bash
cd iOS/VertuEdge
swift test
```

## Xcode workspace

Open `VertuEdge.xcworkspace` to run on simulator/device and attach XCUITest flows.
